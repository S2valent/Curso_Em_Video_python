#faça um programa que leia o compromimento do cateto aposto e do cateto adjacente de um triângulo retângulo, calcule e mostre o comprimento da hipotenusa#

base = float(input('Informe a base: '))
altura = float(input('Informe a altura:'))
h = (base**2 + altura**2)**(1/2)
print('se a base for {} e a altura {} a hipotenusa é {:.2f}'.format(base,altura,h))

import math 
co = float(input('Informe o cateto aposto: '))
ca = float(input('Informe o cateto adjacente: '))
h = math.hypot(co,ca)
print('A hipotenuza é {:.2f}'.format(h))